
How to install:
    1. Download the project, activate virtual environment (will work without it)
    2. Create in the root directory ".env" file, and add your token in it like this: TOKEN = "YOUR_TOKEN_HERE"
    3. To download all requirement modules, do: pip install -r requirements.txt

